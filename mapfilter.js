const sayilar = [2, 3, 4, 2, 2, 3, 2, 1,];

/* const yenisayilar = sayilar.map(function (sayi) { return sayi * 2; })
console.log(yenisayilar);
 */
sayilar.forEach((sayi) => {
    console.log(2 * sayi);
})

